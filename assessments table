CREATE TABLE assessments (
    id INT AUTO_INCREMENT PRIMARY KEY,
    user_id INT NOT NULL,
    fitness_level VARCHAR(20) NOT NULL,
    -- Additional columns for assessment data
    FOREIGN KEY (user_id) REFERENCES users(id)
);
